"""Friendship helpers — STUB.

The friend graph (requests, accept/reject/remove, listings, online status) is
task **C-05..C-07**. The one function other lanes need is ``friendship_state_for``,
which the profile page (A-06) embeds. Until C-07 ships it always reports "none".
"""
import uuid

from sqlalchemy.ext.asyncio import AsyncSession


async def friendship_state_for(
    db: AsyncSession, caller, other_id: uuid.UUID
) -> tuple[str, uuid.UUID | None]:
    return "none", None